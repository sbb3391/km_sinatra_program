<!DOCTYPE html>
<html>
<head>

</head>
<body>
  <h1>Here are all the proposals associated with <%= @user.username %>:</h1>
  
  <ul>
  <% @user.proposals.each do |proposal| %>
    <li><%= Account.find(proposal.account_id).name%></li>
    <ul>
      <a href="/proposals/<%= proposal.id %>"><li></li><%= proposal.id %></li></a>
    </ul>
  <% end %>
  </ul>
</body>
</html>